# ColorHugs — environment template.
# NEVER commit real secrets. Safety-critical logic (AI, moderation, payments)
# must run server-side only. See docs/DECISION_LOG.md #44.

NEXT_PUBLIC_SITE_URL=http://localhost:3000
